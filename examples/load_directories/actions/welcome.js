'use strict';

const debug   = require('debug')('lark-router.examples.load_directories.actions.welcome');
const Router  = require('../../..');

debug('loading ...');

module.exports = () => {
    const router = new Router();
};

debug('load!');
